<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.group.sharegram.schedule.mapper.ScheduleMapper">
	
	<select id="selectCalendarList">
		
	</select>
	
	<insert id="insertSchedule" parameterType="ScheduleDTO" >
		INSERT INTO 
   			SCHEDULE
        		(SCHEDULE_NO , EMP_NO , SCHEDULE_TITLE , SCHEDULE_START , SCHEDULE_END  )
    		VALUES
        		(SCH_SEQ.NEXTVAL , #{empNo} , #{scheduleTitle} , #{scheduleStart} , #{scheduleEnd})
	</insert>
</mapper>